<section class="contact" id="contact">
	<div class="text">
		<header>
			<h2 id="contact-heading" class="title">Leave me a message</h2>
			<p>I am always up for a good conversation</p>
		</header>
		<h3 class="subtitle">Contact information</h3>
		<p class="info" aria-label="phone number">
			<i class="fa-solid fa-phone" aria-hidden="true"></i>
			<span>316-871-6128</span>
		</p>
		<p class="info" aria-label="email">
			<i class="fa-solid fa-envelope" aria-hidden="true"></i>
			<span>huynhlbg@mail.uc.edu</span>
		</p>
	</div>
	<div>
		<form class="form" name="contact-form-bhuynhdev" action="/" method="POST" data-netlify="true">
			<!-- To use Netlify form -->
			<input type="hidden" name="form-name" value="contact-form-svelte" />
			<div class="form-section">
				<input type="text" name="name" id="name-input" required />
				<label for="name-input">Name</label>
			</div>
			<div class="form-section">
				<input type="email" name="email" id="email-input" required />
				<label for="email-input">Email</label>
			</div>
			<div class="form-section">
				<textarea name="message" id="message-input" required></textarea>
				<label for="message-input">Message</label>
			</div>
			<div class="form-section">
				<button type="submit" class="submit-button">
					<span class="submit-text">Send!</span>
				</button>
			</div>
		</form>
	</div>
</section>

<style lang="scss">
	.contact {
		display: flex;
		flex-direction: column;
		justify-content: center;
		gap: 1.5em;
		> * {
			width: 100%;
		}
	}

	.text {
		text-align: center;
		.title {
			margin-bottom: 0;
		}
		.subtitle {
			margin: 0.5em 0;
		}

		.info {
			display: flex;
			justify-content: center;
			align-items: center;
			gap: 1em;
			&:not(:last-child) {
				margin-bottom: 0.5em;
			}
		}
	}

	.form {
		text-align: left;
		max-width: 80%;
		margin: auto;
		.form-section {
			display: flex;
			// Need column-reverse because I need to put label after input element
			// So that I can use sibling selector to add asterisk to required labels
			flex-direction: column-reverse;
			margin-bottom: 0.65em;

			*[required] + label::after {
				content: "*";
				margin-left: 5px;
				color: #e32;
			}

			input,
			textarea {
				padding: 0.4em 0.5em;
				width: 100%;
				border: 1.5px solid;
				border-radius: 4px;
			}
		}

		.submit-button {
			cursor: pointer;
			display: block;
			padding: 0.5em 2.5em;
			margin: 1em auto;
			border-radius: 10px;
			background-color: #0f0f0f;
			color: #f5f5f5;
			border: none;
		}

		.submit-text {
			font-size: 1.2rem;
			line-height: 1.5;
			font-weight: 600;
		}
	}

	@media screen and (min-width: 50em) {
		.contact {
			flex-direction: row;
			justify-content: center;
			h2 {
				margin-top: 1em;
				text-align: left;
			}
		}

		.text {
			text-align: left;
			.info {
				justify-content: flex-start;
			}
		}

		.form {
			margin-top: 1.3em;
			width: 100%;
		}
	}

	@media (hover) {
		.submit-button {
			position: relative;
			background-color: transparent !important;
			color: #0f0f0f !important;
			&::before {
				content: "";
				position: absolute;
				z-index: -1;
				top: 0;
				left: 0;
				display: block;
				border-radius: 1.5em;
				background: rgba(#0f0f0f, 0.4);
				height: 2.9em;
				width: 2.9em;
				transition: all 350ms ease;
			}

			&:hover,
			&:focus-within {
				color: #f5f5f5 !important;
				&:before {
					width: 100%;
					background-color: #0f0f0f;
				}
			}
			&:active {
				transform: scale(0.96);
			}
		}
	}
</style>
